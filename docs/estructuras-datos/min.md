# Función `min`

```python
min([1, 5, 3])         # 1
min((1, 5, 3))         # 1
min("Python")          # 'P'
min(range(5))          # 0
min({"a": 1, "b": 2})  # 'a'
min({1, 5, 3})         # 1
```

```python
min(["n", "abc", "xy"], key=len)  # 'n'
```
