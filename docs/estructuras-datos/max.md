# Función `max`

```python
max([1, 5, 3])         # 5
max((1, 5, 3))         # 5
max("Python")          # 'y'
max(range(5))          # 4
max({"a": 1, "b": 2})  # 'b'
max({1, 5, 3})         # 5
```

```python
max(["n", "abc", "xy"], key=len)  # 'abc'
```
