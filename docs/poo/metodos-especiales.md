# Métodos especiales

- `__init__()` → Inicialización del objeto
- `__str__()`  → Representación legible
- `__repr__()` → Representación para desarrolladores
- `__len__()`  → Define qué devuelve `len()`
- `__eq__()`   → Define `==`
- `__lt__()`   → Define `<`
- `__add__()`  → Define `+`
