# Instrucción `break`

```py
numeros = [3, 5, -2, 1, 4]

for n in numeros:
    if n < 0:
        print(f"Negativo encontrado: {n}. Deteniendo bucle.")
        break
    print(f"Número válido: {n}")
```
